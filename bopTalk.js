let number
let message = 'compsci is so cool'
let part = Text.slice(x,y)
let vowel

if (part = b || c || d || f || g || h || j || k || l || m || n || p || q || r || s || t || v || w || x || y || z ) {
    part = consenent
    console.log(consenent + "op")

} else {
    console.log(part)
}

